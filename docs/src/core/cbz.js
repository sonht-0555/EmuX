window.extractCBZ = extractCBZ;async function extractCBZ(arrayBuffer, romName) { const imgExts = /\.(jpe?g|png|webp|gif|avif)$/i; await showNotification("", "##", "-", "", true); const files = fflate.unzipSync(new Uint8Array(arrayBuffer)); const names = Object.keys(files).filter(n => imgExts.test(n) && files[n].length > 0).sort(); const urls = names.map(name => URL.createObjectURL(new Blob([files[name]]))); let page = Number(localStorage.getItem(`page_${romName}`)) || 0, isInitialScroll = true; screen.innerHTML = ''; const manga = document.createElement('manga'); manga.innerHTML = urls.map(u => `<img src="${u}" loading="lazy">`).join(''); screen.appendChild(manga); const restoreScroll = () => {if (page > 0 && manga.scrollHeight > manga.clientHeight) manga.scrollTop = page * (manga.scrollHeight - manga.clientHeight);}; page02.hidden = false; await showNotification("", "###", "", "", true); manga.querySelectorAll('img').forEach(img => img.onload = restoreScroll); setTimeout(() => { restoreScroll(); body.classList.add('cbz-open'); isInitialScroll = false; [bpad, dpad, jpad, page00, page01, switch0].forEach((el) => (el.hidden = true)); }, 200); manga.onscroll = () => { if (isInitialScroll) return; const total = manga.scrollHeight - manga.clientHeight; if (total > 0) {const isAtEnd = manga.scrollTop >= total; page = isAtEnd ? 0 : manga.scrollTop / total; localStorage.setItem(`page_${romName}`, page);} }; manga.ontouchstart = manga.ontouchmove = e => e.stopPropagation();}