export function createLevel() { let level = ''; const toClasses = value => String(value || '').trim().split(/\s+/).filter(Boolean) .map(v => v.startsWith('manga-') ? v : `manga-${v}`); const set = (value, save) => { body.classList.remove(...Array.from(body.classList).filter(v => v.startsWith('manga-'))); body.classList.add(...toClasses(level = String(value || '').trim())); if (save) local('manga_level', level); }; set(local('manga_level') || ''); return {set, get value() {return level;}};}