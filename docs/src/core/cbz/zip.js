const IMG_EXT = /\.(jpe?g|png|webp|gif|avif)$/i;const MIME = {jpg: 'image/jpeg', jpeg: 'image/jpeg', png: 'image/png', webp: 'image/webp', gif: 'image/gif', avif: 'image/avif'};export const HEADER_BYTES = 32768; export const isScan = name => name.endsWith('.scan') || name.includes('.scan.');export const shortName = name => name.split('/').pop().replace(/\.[^/.]+$/, '');const mimeOf = name => MIME[name.split('.').pop().toLowerCase()] || 'image/jpeg';export function imageSize(b) { if (!b || b.length < 16) return null; const be16 = p => (b[p] << 8) | b[p + 1]; const be32 = p => ((b[p] << 24) | (b[p + 1] << 16) | (b[p + 2] << 8) | b[p + 3]) >>> 0; const le16 = p => b[p] | (b[p + 1] << 8); const tag = (p, s) => { for (let i = 0; i < s.length; i++) if (b[p + i] !== s.charCodeAt(i)) return false; return true; }; if (be32(0) === 0x89504e47) return {w: be32(16), h: be32(20)}; if (tag(0, 'GIF8')) return {w: le16(6), h: le16(8)}; if (b[0] === 0xFF && b[1] === 0xD8) { for (let p = 2; p + 9 < b.length;) { if (b[p] !== 0xFF || b[p + 1] === 0xFF) {p++; continue;} const m = b[p + 1]; if (m === 0x01 || (m >= 0xD0 && m <= 0xD9)) {p += 2; continue;} if (m >= 0xC0 && m <= 0xCF && m !== 0xC4 && m !== 0xC8 && m !== 0xCC) return {w: be16(p + 7), h: be16(p + 5)}; const size = be16(p + 2); if (size < 2) return null; p += 2 + size; } return null; } if (tag(0, 'RIFF') && tag(8, 'WEBP')) { if (tag(12, 'VP8X')) return {w: (b[24] | b[25] << 8 | b[26] << 16) + 1, h: (b[27] | b[28] << 8 | b[29] << 16) + 1}; if (tag(12, 'VP8L')) { const n = (b[21] | b[22] << 8 | b[23] << 16 | b[24] << 24) >>> 0; return {w: (n & 0x3FFF) + 1, h: ((n >>> 14) & 0x3FFF) + 1}; } if (tag(12, 'VP8 ')) return {w: le16(26) & 0x3FFF, h: le16(28) & 0x3FFF}; } if (tag(4, 'ftyp')) { for (let p = 4, end = Math.min(b.length - 12, 4096); p < end; p++) { if (tag(p, 'ispe')) return {w: be32(p + 8), h: be32(p + 12)}; } } return null;}export function openZip(data) { const u8 = data instanceof Uint8Array ? data : new Uint8Array(data); const {buffer, byteOffset, length: len} = u8, view = new DataView(buffer, byteOffset, len); let eocd = -1; for (let i = len - 22, min = Math.max(0, len - 65558); i >= min; i--) { if (view.getUint32(i, true) === 0x06054b50) {eocd = i; break;} } if (eocd === -1) throw new Error("Không đúng chuẩn file ZIP!"); const cdSize = view.getUint32(eocd + 12, true), cdOffset = view.getUint32(eocd + 16, true); const decoder = new TextDecoder('utf-8'), files = []; let json = null; for (let p = cdOffset, end = cdOffset + cdSize; p < end;) { if (view.getUint32(p, true) !== 0x02014b50) break; const nameLen = view.getUint16(p + 28, true); const entry = { name: decoder.decode(u8.subarray(p + 46, p + 46 + nameLen)), method: view.getUint16(p + 10, true), csize: view.getUint32(p + 20, true), size: view.getUint32(p + 24, true), offset: view.getUint32(p + 42, true), }; if (!entry.name.startsWith('__MACOSX') && !entry.name.endsWith('/')) { if (entry.name.endsWith('.json')) {if (!json) json = entry;} else if (IMG_EXT.test(entry.name) || isScan(entry.name)) files.push(entry); } p += 46 + nameLen + view.getUint16(p + 30, true) + view.getUint16(p + 32, true); } const rawBytes = entry => { const lp = entry.offset; if (view.getUint32(lp, true) !== 0x04034b50) throw new Error("Lỗi Local Header Offset: " + entry.name); const from = lp + 30 + view.getUint16(lp + 26, true) + view.getUint16(lp + 28, true); return u8.subarray(from, from + entry.csize); }; const extract = entry => { const raw = rawBytes(entry), type = mimeOf(entry.name); if (entry.method === 0) return new Blob([raw], {type}); if (entry.method === 8) return new Blob([fflate.inflateSync(raw)], {type}); throw new Error("Chưa hỗ trợ compression method: " + entry.method); }; const prefix = (entry, limit) => { let raw; try {raw = rawBytes(entry);} catch (err) {return null;} if (entry.method === 0) return raw.subarray(0, Math.min(raw.length, limit)); if (entry.method !== 8) return null; const parts = []; let total = 0; try { const inf = new fflate.Inflate(chunk => {parts.push(chunk); total += chunk.length;}); for (let p = 0; p < raw.length && total < limit; p += 8192) inf.push(raw.subarray(p, p + 8192), false); } catch (err) { /* stream cụt là chuyện bình thường, dùng những gì đã ra */ } if (!total) return null; const out = new Uint8Array(Math.min(total, limit)); let o = 0; for (const part of parts) { if (o >= out.length) break; out.set(part.subarray(0, out.length - o), o); o += part.length; } return out; }; const collator = new Intl.Collator(undefined, {numeric: true, sensitivity: 'base'}); files.sort((a, b) => collator.compare(a.name, b.name)); return {files, json, extract, prefix};}