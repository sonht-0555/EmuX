let databaseCache = null;const DATABASE_NAME = 'EmuxDB', STORES = { games: ['nes', 'sfc', 'smc', 'gba', 'gb', 'gbc', 'bin', 'rom', 'md', 'gen', 'ngp', 'ngc', 'nds', 'iso', 'img', 'cue', 'pbp', 'zip', 'pce'], saves: ['sav', 'srm', 'sram'], states: ['ss1', 'ss2', 'ss3', 'ss4', 'ss5', 'ss6', 'ss7', 'ss8', 'ss9', 'ss0']};const apiEndpoint = "https://api.github.com/repos/AlexBlackmore/roms/git/trees/master?recursive=1";const rawContentBase = "https://raw.githubusercontent.com/AlexBlackmore/roms/master/";let storeList = [];const getAllStoreNames = () => Object.keys(STORES);function storeForFilename(filename) { if (!filename || typeof filename !== 'string') return 'games'; const ext = filename.split('.').pop()?.toLowerCase(); return (ext && getAllStoreNames().find(storeName => STORES[storeName].includes(ext))) || 'games';}async function getDB() { if (databaseCache && databaseCache.readyState !== 'closing') return databaseCache; const openDB = version => new Promise((resolve, reject) => { const request = indexedDB.open(DATABASE_NAME, version); request.onupgradeneeded = event => { const db = event.target.result; getAllStoreNames().forEach(s => !db.objectStoreNames.contains(s) && db.createObjectStore(s)); }; request.onsuccess = event => { const db = event.target.result; db.onversionchange = () => {db.close(); databaseCache = null;}; resolve(db); }; request.onerror = reject; }); try { const db = await openDB(); if (getAllStoreNames().some(s => !db.objectStoreNames.contains(s))) { db.close(); return databaseCache = await openDB(db.version + 1); } return databaseCache = db; } catch (e) { databaseCache = null; throw e; }}async function emuxDB(dataOrKey, name) { const db = await getDB(), key = name || dataOrKey, store = storeForFilename(String(key)); if (name && store === 'games') { let order = JSON.parse(localStorage.getItem('emux_order') || '[]'); if (!order.includes(name)) { order.push(name); localStorage.setItem('emux_order', JSON.stringify(order)); } } const transaction = db.transaction(store, name ? 'readwrite' : 'readonly'), objectStore = transaction.objectStore(store); return new Promise((resolve, reject) => { if (name) { objectStore.put(dataOrKey instanceof ArrayBuffer ? new Uint8Array(dataOrKey) : dataOrKey, name); transaction.oncomplete = () => resolve(true); transaction.onerror = event => reject(event); } else { const request = objectStore.get(dataOrKey); request.onsuccess = () => resolve(request.result); request.onerror = event => reject(event); } });}async function listStore(storeName) { const db = await getDB(); const getKeys = store => new Promise((resolve, reject) => { if (!db.objectStoreNames.contains(store)) return resolve([]); const request = db.transaction(store, 'readonly').objectStore(store).getAllKeys(); request.onsuccess = () => { let keys = request.result.map(String); if (store === 'games') { const order = JSON.parse(localStorage.getItem('emux_order') || '[]'); keys.sort((a, b) => { let idxA = order.indexOf(a), idxB = order.indexOf(b); return (idxA === -1 ? 9999 : idxA) - (idxB === -1 ? 9999 : idxB); }); } resolve(keys); }; request.onerror = reject; }); if (!storeName) { const names = getAllStoreNames(), keys = await Promise.all(names.map(getKeys)); return names.reduce((accumulator, name, index) => ({...accumulator, [name]: keys[index]}), {}); } return getKeys(storeName);}async function deleteFromStore(key) { const db = await getDB(), store = storeForFilename(String(key)); if (store === 'games') { let order = JSON.parse(localStorage.getItem('emux_order') || '[]'); localStorage.setItem('emux_order', JSON.stringify(order.filter(k => k !== key))); } return new Promise((resolve, reject) => { const transaction = db.transaction(store, 'readwrite'), objectStore = transaction.objectStore(store); objectStore.delete(key); transaction.oncomplete = () => resolve(true); transaction.onerror = event => reject(event); });}async function renameFromStore(oldName, newName) { if (!oldName || !newName || oldName === newName) return false; const oldBase = oldName.includes('.') ? oldName.substring(0, oldName.lastIndexOf('.')) : oldName; const newBase = newName.includes('.') ? newName.substring(0, newName.lastIndexOf('.')) : newName; const stores = getAllStoreNames(); for (const store of stores) { const keys = await listStore(store); const targets = keys.filter(k => k === oldBase || k.startsWith(oldBase + ".")); for (const key of targets) { const suffix = key.slice(oldBase.length), targetKey = newBase + suffix; if (store === 'games') { let order = JSON.parse(localStorage.getItem('emux_order') || '[]'); const idx = order.indexOf(key); if (idx !== -1) { order[idx] = targetKey; localStorage.setItem('emux_order', JSON.stringify(order)); } } const data = await emuxDB(key); if (data) { await emuxDB(data, targetKey); await deleteFromStore(key); } } } return true;}async function downloadFromStore(name) { const data = await emuxDB(name); if (!data) return message("File not found!"); const url = URL.createObjectURL(new Blob([data])), link = document.createElement('a'); link.href = url; link.download = name; link.click(); URL.revokeObjectURL(url);}async function initStore() { try { const response = await fetch(apiEndpoint); const data = await response.json(); storeList = data.tree.filter(item => item.type === 'blob'); } catch (e) { }}