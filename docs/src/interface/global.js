const tags = ["html", "body", "page00", "page01", "page02", "notification", "display", "list", "list01", "list02", "name", "ver", "title1", "vertical", "screen", "invis", "message0", "skip1", "switch0", "title0", "logo", "jpad", "setting", "cta", "log"];tags.forEach(selector => window[selector] = document.querySelector(selector));const local = (key, value) => (value === undefined || value === null) ? localStorage.getItem(key) : localStorage.setItem(key, value);const delay = milliseconds => new Promise(resolve => setTimeout(resolve, milliseconds));let gameName, gameWidth, gameHeight, integer, timerId, time1Element, count = null, recCount = 1, canvasBottom, hours = 0, minutes = 0, seconds = 0, count1 = 0, current = parseInt(local('vertical')) || 0;const canvas = document.getElementById('canvas');function doubleTap(event, element, distance) { const now = Date.now(), deltaTime = now - (element._lastTime || 0), isTiming = event.isPrimary && deltaTime < 300 && deltaTime > 40; let isDistance = true; if (distance) isDistance = Math.hypot(event.clientX - element._lastX, event.clientY - element._lastY) < 30; const result = isTiming && isDistance; element._lastTime = now; element._lastX = event.clientX; element._lastY = event.clientY; return result;}function generateSvgPattern(repetitions, size, pattern) { const total = repetitions * size, cells = typeof pattern === 'number' ? Array.from({length: pattern * pattern}, (_, index) => index % (pattern + 1) ? 0 : 1) : pattern.replace(/\s/g, '').split('.'); const patternSize = Math.sqrt(cells.length); let svg = `<svg xmlns='http://www.w3.org/2000/svg' width='${total}' height='${total}'>`; for (let index = 0; index < total * total; index++) { const x = index % total, y = (index / total) | 0, cellIndex = ((y % size * patternSize / size) | 0) * patternSize + ((x % size * patternSize / size) | 0); if (cells[cellIndex] == 1) svg += `<rect x="${x}" y="${y}" width="1" height="1"/>`; } return `url("data:image/svg+xml,${encodeURIComponent(svg + "</svg>")}")`;}async function showNotification(base, white, gray, text, wait) { page00.hidden = false; title0.setAttribute('base', base); title0.setAttribute('gray', gray); title0.textContent = white; message0.textContent = text; if (wait) { window._loadDelay = 400; while (window._loadDelay > 0) {await delay(100); window._loadDelay -= 100;} }}async function message(text, duration = 2000) { if (log.style.opacity === '1') log.style.display = "none"; if (count) count.cancelled = true; const token = {cancelled: false}; count = token; const fullText = text.charAt(0).toUpperCase() + text.slice(1); title1.textContent = ""; for (const char of fullText) { if (token.cancelled) return; title1.textContent += char; await delay(75); } await delay(duration); if (!token.cancelled && count === token) { title1.textContent = ""; count = null; await delay(200); if (log.style.opacity === '1') log.style.display = "block"; }}async function gameView(name) { if (!name) return; const styles = getComputedStyle(document.documentElement); const safeLeft = parseFloat(styles.getPropertyValue('--safe-left')) || 0; const safeRight = parseFloat(styles.getPropertyValue('--safe-right')) || 0; switch0.textContent = local('render'); const maxInteger = Math.floor(window.innerWidth < window.innerHeight ? (window.innerWidth * window.devicePixelRatio) / canvas.width : (window.innerWidth >= 1024 && matchMedia('(pointer: fine)').matches) ? (window.innerHeight * window.devicePixelRatio) / canvas.height : Math.min(((window.innerWidth - ((Math.floor((window.innerHeight * 0.5 - 13) / 4) * 4) * 2) - 17 - Math.max(0, safeLeft - 10) - Math.max(0, safeRight - 10)) * window.devicePixelRatio) / canvas.width, (window.innerHeight * window.devicePixelRatio) / canvas.height)); integer = (maxInteger > 6) ? maxInteger - (maxInteger % 2) : maxInteger; const ratio = integer / window.devicePixelRatio; display.style.cssText = `height:${Math.ceil(canvas.height * ratio)}px;width:${Math.ceil(canvas.width * ratio)}px`; screen.style.width = `${(canvas.width * ratio)}px`; screen.style.setProperty("--size", `${integer}px`); screen.style.setProperty("--width", `${canvas.width * integer}px`); screen.style.setProperty("--height", `${canvas.height * integer}px`); screen.style.setProperty("--scale", ratio / integer); await delay(300); [page01, list01, list02, switch0].forEach(page => page.hidden = true); if (isRunning) page00.hidden = true; [page02, list].forEach(page => page.hidden = false); const patternSize = (integer <= 4 || integer % 2 !== 0) ? integer : (integer / 2); screen.style.setProperty("--shader", generateSvgPattern(integer / patternSize, patternSize, local(`shader0${local("shader")}`) || patternSize)); message(name);}window.addEventListener("resize", () => gameView(gameName));document.addEventListener("DOMContentLoaded", () => { body.removeAttribute('hide'); [page00, page02].forEach(el => el.ontouchstart = el.ontouchmove = el.oncontextmenu = e => e.preventDefault()); canvasBottom = document.getElementById("canvas-bottom"); body.style.setProperty("--background", generateSvgPattern(1, window.devicePixelRatio, window.devicePixelRatio));});